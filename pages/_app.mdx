import '../styles/main.css';
import { Analytics } from '@vercel/analytics/react';

export default function MyApp({ Component, pageProps }) {
    return (
            <>
                <Analytics id={process.env.NEXT_PUBLIC_VERCEL_API}/>
                <Component {...pageProps} />
            </>
    )
}